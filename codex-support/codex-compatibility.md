# Codex compatibility (moved)

The compatibility contract is now host-neutral and lives at
`host-support/compatibility.md`. Apply that document with host key `codex`;
the resolver is `host-support/resolve-runtime.mjs` (`--host codex`, the
default). This file remains so previously installed Codex adapters and
documentation that reference this path still resolve.
