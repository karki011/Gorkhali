{
  "skill_name": "phantom",
  "version": "2.1",
  "created": "2026-05-23",
  "evals": [
    {
      "id": 1,
      "skill": "phantom:start",
      "prompt": "ok so I need to add a date picker to the settings page, the Jira ticket is CP-4521 and the PM wants it by Thursday",
      "should_trigger": true,
      "expected_behavior": "Parse CP-4521, fetch Jira ticket, create session state, decompose into phases, spawn shadows agents for implementation"
    },
    {
      "id": 2,
      "skill": "phantom:start",
      "prompt": "Please implement the multi-currency support feature described in CLOUD-8834. It affects the billing module and we need to handle EUR, GBP, and JPY alongside USD.",
      "should_trigger": true,
      "expected_behavior": "Parse CLOUD-8834, load Jira context, detect feature (not bug), create plan with blast radius check on billing module, execute multi-agent shadows"
    },
    {
      "id": 3,
      "skill": "phantom:start",
      "prompt": "what's the status of the CP-4521 work?",
      "should_trigger": false,
      "expected_behavior": "Should route to phantom:status — user wants current state, not to start new work. phantom:start would create a new session when one already exists."
    },

    {
      "id": 4,
      "skill": "phantom:verify",
      "prompt": "does it pass? run the tests and lint before I push",
      "should_trigger": true,
      "expected_behavior": "Discover verify commands from repo CLAUDE.md or package.json, run lint + build + tests, report pass/fail, run power level if all pass"
    },
    {
      "id": 5,
      "skill": "phantom:verify",
      "prompt": "Please run the full verification suite against the changes I just made to the billing module. I need lint, typecheck, and unit tests to all be green before we proceed.",
      "should_trigger": true,
      "expected_behavior": "Run repo verification commands (pnpm lint, pnpm typecheck:tsc, pnpm test), run simplify on changed files, spawn power level agent on pass"
    },
    {
      "id": 6,
      "skill": "phantom:verify",
      "prompt": "can you fix the test that's failing in billing.test.ts?",
      "should_trigger": false,
      "expected_behavior": "Should route to phantom:fix — user wants repair, not verification. Verify reports status; fix repairs failures."
    },

    {
      "id": 7,
      "skill": "phantom:fix",
      "prompt": "tests are failing, build is broken — fix it",
      "should_trigger": true,
      "expected_behavior": "Load failure details from verification.json, triage failures by type, spawn scoped repair agents, re-run phantom:verify after repairs"
    },
    {
      "id": 8,
      "skill": "phantom:fix",
      "prompt": "The TypeScript build errors from the last verify run need to be addressed. There are 3 type errors in packages/billing/src/currency.ts and a lint violation in apps/dashboard/src/components/CurrencyPicker.tsx",
      "should_trigger": true,
      "expected_behavior": "Load verification artifact, classify as type + lint failures, spawn repair agents scoped to those files, re-verify after repairs"
    },
    {
      "id": 9,
      "skill": "phantom:fix",
      "prompt": "I think there might be a bug in the currency formatter — can you investigate what's going wrong?",
      "should_trigger": false,
      "expected_behavior": "Should route to phantom:hound — this is an unknown bug requiring investigation, not a known verification failure with a fix packet"
    },

    {
      "id": 10,
      "skill": "phantom:review",
      "prompt": "review this before I ship it — I want a second opinion on the approach",
      "should_trigger": true,
      "expected_behavior": "Load session contracts, spawn Gaze agent (opus) with touched files, produce CRITICAL/WARNING/INFO findings, return APPROVED or NEEDS WORK verdict"
    },
    {
      "id": 11,
      "skill": "phantom:review",
      "prompt": "Can you do a full code quality review of the changes? I'm particularly concerned about whether we're following KISS/DRY principles and if there's any code that could be simplified.",
      "should_trigger": true,
      "expected_behavior": "Run Gaze in gauntlet mode: simplify → full review → verify. Check KISS/DRY, flag architecture concerns, produce file:line specific findings"
    },
    {
      "id": 12,
      "skill": "phantom:review",
      "prompt": "run the tests and check if the build passes",
      "should_trigger": false,
      "expected_behavior": "Should route to phantom:verify — user wants correctness checks (tests/build), not a quality/architecture review from Gaze"
    },

    {
      "id": 13,
      "skill": "phantom:hound",
      "prompt": "users are reporting that invoice totals are wrong for EUR customers — started happening after Tuesday's deploy, no error in logs",
      "should_trigger": true,
      "expected_behavior": "Run 7-step investigation: collect symptoms, build timeline (check Tuesday deploy via git log), trace code path through currency/invoice logic, form hypothesis, gather evidence, confirm root cause, produce HTML report"
    },
    {
      "id": 14,
      "skill": "phantom:hound",
      "prompt": "investigate why the CurrencyFormatter class is producing NaN for certain JPY amounts — I suspect it's something in the rounding logic but I'm not sure",
      "should_trigger": true,
      "expected_behavior": "Start with symptoms (NaN for JPY), check git blame on CurrencyFormatter, trace through rounding logic, form and test hypothesis, write HTML investigation report"
    },
    {
      "id": 15,
      "skill": "phantom:hound",
      "prompt": "fix the NaN bug in CurrencyFormatter",
      "should_trigger": false,
      "expected_behavior": "Should route to phantom:fix — user already knows the issue and wants it fixed. Hound is for unknown failures requiring forensic investigation."
    },

    {
      "id": 16,
      "skill": "phantom:wrap",
      "prompt": "ok we're done, ship it — create the PR",
      "should_trigger": true,
      "expected_behavior": "Check verification.json for passing verdict, run diff-against-main scope check, run shadows eval, save learnings, create PR with branch push"
    },
    {
      "id": 17,
      "skill": "phantom:wrap",
      "prompt": "I'm satisfied with the implementation of CP-4521, please finalize the session, record what we learned, and open a pull request against main",
      "should_trigger": true,
      "expected_behavior": "Verify state artifact exists and verdict is pass, run pre-wrap hook, check scope creep via git diff main...HEAD, record learnings, create PR"
    },
    {
      "id": 18,
      "skill": "phantom:wrap",
      "prompt": "can you push this branch so I can review it locally on another machine?",
      "should_trigger": false,
      "expected_behavior": "A simple git push should not trigger the full wrap ceremony — wrap runs eval, scope checks, learning recording. User just wants a branch push."
    },

    {
      "id": 19,
      "skill": "phantom:scout",
      "prompt": "before we start building the currency picker component, can you explore how other pickers are implemented in the codebase and what design tokens they use?",
      "should_trigger": true,
      "expected_behavior": "Spawn parallel explorer agents for design (component patterns, tokens), api (existing hooks/clients), patterns (state management patterns used in similar pickers)"
    },
    {
      "id": 20,
      "skill": "phantom:scout",
      "prompt": "what does the billing module do and how does it connect to the invoicing service?",
      "should_trigger": true,
      "expected_behavior": "Auto-detect relevant scouting areas (deps, api), spawn background agents to map billing → invoicing relationships, API contracts, shared types"
    },
    {
      "id": 21,
      "skill": "phantom:scout",
      "prompt": "implement a currency switcher in the nav bar",
      "should_trigger": false,
      "expected_behavior": "Should route to phantom:start — user wants to build something, not gather context. Scout is for exploration before planning, not for kicking off implementation."
    },

    {
      "id": 22,
      "skill": "phantom:recruit",
      "prompt": "I need an accessibility specialist to audit the new currency picker — focus on keyboard nav and screen reader support",
      "should_trigger": true,
      "expected_behavior": "Parse role focus as 'accessibility specialist', spawn Blade with A11Y ROLE FOCUS directive, scoped to currency picker files, report findings via Post-Agent Hook"
    },
    {
      "id": 23,
      "skill": "phantom:recruit",
      "prompt": "get me a performance expert to look at why the billing dashboard is loading slowly — I want someone focused on bundle size and render cost",
      "should_trigger": true,
      "expected_behavior": "Parse role focus as 'performance specialist', spawn Blade to analyze bundle size, render performance, lazy loading opportunities in billing dashboard"
    },
    {
      "id": 24,
      "skill": "phantom:recruit",
      "prompt": "do a code review of the currency formatter",
      "should_trigger": false,
      "expected_behavior": "Should route to phantom:review — a general code review is Gaze's job. Recruit is for specialized one-off agents with a specific ROLE FOCUS beyond normal shadows."
    },

    {
      "id": 25,
      "skill": "phantom:pause",
      "prompt": "I gotta jump to another meeting, save where we are on CP-4521",
      "should_trigger": true,
      "expected_behavior": "Capture git state (branch, HEAD, uncommitted files), write pause-state.json with current phase and artifact paths, output resume command"
    },
    {
      "id": 26,
      "skill": "phantom:pause",
      "prompt": "Pausing for the day — please save the full session state for CP-4521 including what phase we're in, what's been completed, and what's still pending",
      "should_trigger": true,
      "expected_behavior": "Write complete pause-state.json artifact with git state, current phase, completed artifacts, pending work, and resume instructions"
    },
    {
      "id": 27,
      "skill": "phantom:pause",
      "prompt": "commit my work and push the branch",
      "should_trigger": false,
      "expected_behavior": "Should route to phantom:wrap — committing and pushing is the wrap ceremony. Pause only saves state artifacts; no git operations happen during pause."
    },

    {
      "id": 28,
      "skill": "phantom:resume",
      "prompt": "resuming CP-4521, pick up where we left off",
      "should_trigger": true,
      "expected_behavior": "Read state/sessions/CP-4521/pause-state.json, check git HEAD staleness, restore intent + plan + contracts, display current phase and pending tasks"
    },
    {
      "id": 29,
      "skill": "phantom:resume",
      "prompt": "Continue working on CLOUD-8834 — I was in the middle of implementing the EUR support when we stopped yesterday",
      "should_trigger": true,
      "expected_behavior": "Load CLOUD-8834 pause-state.json, perform staleness check (yesterday's HEAD vs current), restore full session context, resume from last recorded phase"
    },
    {
      "id": 30,
      "skill": "phantom:resume",
      "prompt": "start working on CP-4521 again, we need to add GBP support",
      "should_trigger": false,
      "expected_behavior": "Should route to phantom:start — user wants to add new scope (GBP support), not just resume saved state. Resume restores context; start creates new work scope."
    },

    {
      "id": 31,
      "kind": "route",
      "skill": "phantom:start",
      "prompt": "ENG-123: there's a typo in the empty state on the settings page — 'Recieve notifications' should be 'Receive notifications', it's in EmptyState.tsx",
      "expected_route": "DIRECT",
      "expected_behavior": "Parse ENG-123 as the ticket via the generic [A-Z][A-Z0-9]+-\\d+ match (no hardcoded project prefix), classify DIRECT (single known file, known pattern, confidence >=0.9), report '[DIRECT] {rationale}' with zero human gates, spawn a Blade immediately"
    },
    {
      "id": 32,
      "kind": "route",
      "skill": "phantom:start",
      "prompt": "rename the formatTotal helper to formatInvoiceTotal in src/utils/invoice.ts — all call sites are in that same file",
      "expected_route": "DIRECT",
      "expected_behavior": "Single-file mechanical rename with clear scope and a known pattern → DIRECT. Transcript shows the '[DIRECT] {rationale}' route report and a Blade spawn with no plan-approval gate"
    },
    {
      "id": 33,
      "kind": "route",
      "skill": "phantom:start",
      "prompt": "PLAT-4567: add a CSV export button to the cost report table. AC: button sits in the table toolbar, export respects the active filters, file downloads as UTF-8 CSV. It mirrors the existing PDF export — touches the toolbar, the table container, a new export util, and the download hook.",
      "expected_route": "PLAN",
      "expected_behavior": "Parse PLAT-4567 via the generic ticket regex. Clear scope, ~4 files, known approach (mirrors existing PDF export), confidence >=0.7 → PLAN. Transcript shows '[PLAN] {rationale}', Planner↔Challenger deliberation, and exactly one human gate (plan approval) before execution"
    },
    {
      "id": 34,
      "kind": "route",
      "skill": "phantom:start",
      "prompt": "the analytics dashboard feels slow and clunky — improve the loading experience somehow. not sure if we want skeletons, prefetching, streaming, or caching. open to ideas",
      "expected_route": "BRAINSTORM",
      "expected_behavior": "Ambiguous scope ('somehow'), competing patterns (skeletons vs prefetch vs streaming vs caching), confidence <0.7 → BRAINSTORM. Transcript shows '[BRAINSTORM] {rationale}', invokes Skill(skill=\"phantom:brainstorm\"), and presents a direction gate before any plan gate (2 gates total)"
    },
    {
      "id": 35,
      "kind": "route",
      "skill": "phantom:start",
      "prompt": "ACME-89: migrate authentication from session cookies to OAuth2 with PKCE across the gateway, web app, and CLI — auth middleware, token storage, the login UI, and the public token endpoints all change",
      "expected_route": "FULL",
      "expected_behavior": "Parse ACME-89 via the generic ticket regex. 10+ files across 3+ packages, cross-layer, security + public-API surface → FULL. Transcript shows '[FULL] {rationale}' and three human gates in order: direction (brainstorm), plan, and wiring via Skill(skill=\"phantom:wire\")"
    },
    {
      "id": 36,
      "kind": "route",
      "skill": "phantom:start",
      "prompt": "a teammate logged abc-123 in our internal tracker — it's just a typo: 'occured' should be 'occurred' in the error toast in Toast.tsx",
      "expected_route": "DIRECT",
      "expected_behavior": "Lowercase abc-123 must NOT match the ticket pattern [A-Z][A-Z0-9]+-\\d+ — no Jira fetch is attempted for abc-123; ticket resolution falls back to the current branch or the no-ticket flow. The single-file typo still classifies DIRECT with a '[DIRECT] {rationale}' report"
    },

    {
      "id": 37,
      "kind": "convention",
      "skill": "phantom:start",
      "prompt": "implement retry-with-backoff in the sync client — there are at least two reasonable designs (queue-based vs decorator), pick the right one",
      "setup": "config.yaml at ${PHANTOM_DATA:-~/.claude/phantom-data}/config.yaml contains:\nmodels:\n  sage: opus\nThe Blade hits a hard decision (2+ viable approaches) mid-task and escalates to Sage.",
      "expected_check": {
        "type": "regex",
        "pattern": "subagent_type[\"':\\s]*\"?sage\"?[\\s\\S]{0,300}?model[\"':\\s]*\"?opus\"?"
      },
      "expected_behavior": "Blade reads config.yaml before spawning Sage; the Agent-tool spawn params in the transcript show subagent_type sage WITH model opus passed as the override"
    },
    {
      "id": 38,
      "kind": "convention",
      "skill": "phantom:start",
      "prompt": "implement retry-with-backoff in the sync client — there are at least two reasonable designs (queue-based vs decorator), pick the right one",
      "setup": "config.yaml exists but contains no models: key (or config.yaml is missing entirely). The Blade escalates to Sage mid-task.",
      "expected_check": {
        "type": "regex",
        "pattern": "^(?![\\s\\S]*subagent_type[\"':\\s]*\"?sage\"?[\\s\\S]{0,300}?\"?\\bmodel\\b\"?\\s*:)[\\s\\S]*subagent_type[\"':\\s]*\"?sage\"?"
      },
      "expected_behavior": "The Sage spawn in the transcript omits the model param entirely, so sage.md's front-matter pin (model: fable) applies — no model: line appears in the spawn params"
    },
    {
      "id": 39,
      "kind": "convention",
      "skill": "phantom:verify",
      "prompt": "does it pass? run the full verification suite before I push",
      "setup": "Repo root contains yarn.lock (no pnpm-lock.yaml, no bun.lockb, no package-lock.json). Neither CLAUDE.md nor package.json scripts define explicit test/lint/build commands, so tier-3 stack defaults apply.",
      "expected_check": {
        "type": "regex",
        "pattern": "^(?![\\s\\S]*\\bpnpm\\b)[\\s\\S]*\\byarn (test|lint|build)\\b"
      },
      "expected_behavior": "Verification plan resolves {TEST_CMD}/{LINT_CMD}/{BUILD_CMD} via the lockfile table to yarn commands (yarn test, yarn lint, yarn build); the string 'pnpm' never appears in the transcript"
    },
    {
      "id": 40,
      "kind": "convention",
      "skill": "phantom:visual",
      "prompt": "visual check the new pricing card — does it look right across viewports?",
      "setup": "vite.config.ts sets server: { port: 5173 }. The dev server is running on 5173. No other port configuration exists.",
      "expected_check": {
        "type": "regex",
        "pattern": "^(?![\\s\\S]*localhost:3000)[\\s\\S]*localhost:5173"
      },
      "expected_behavior": "The visual flow resolves {DEV_PORT} from the dev-server config (vite.config.ts) and Lens navigates to localhost:5173; it never assumes or mentions the fixed default localhost:3000"
    },
    {
      "id": 41,
      "kind": "convention",
      "skill": "phantom:start",
      "prompt": "quick fix: update the copyright year in src/components/Footer.tsx",
      "setup": "Repo's origin/HEAD points at trunk (the default branch is named trunk). No git.protected_branches in config.yaml and PHANTOM_PROTECTED_BRANCHES is unset. Current branch is trunk; an Edit is attempted on src/components/Footer.tsx.",
      "expected_check": {
        "type": "regex",
        "pattern": "BLOCKED: Cannot edit source files on trunk branch"
      },
      "expected_behavior": "feature-branch-gate auto-detects trunk via origin/HEAD (even though the configured fallback list is only main/master/develop) and blocks the Edit, printing the BLOCKED message plus the 'git checkout -b {ticket}/{slug}' instruction"
    },
    {
      "id": 42,
      "kind": "convention",
      "skill": "phantom:greploop",
      "prompt": "wrap it up and create the PR",
      "setup": "config.yaml has no integrations: section — integrations.greptile is absent. A draft PR was just created by wrap, which auto-invokes greploop.",
      "expected_check": {
        "type": "regex",
        "pattern": "greploop skipped \\(integrations\\.greptile not enabled in config\\.yaml\\)"
      },
      "expected_behavior": "Greploop prints the one-line skip note '○ greploop skipped (integrations.greptile not enabled in config.yaml)' and stops — no @greptileai comment is posted and no error is raised; wrap continues normally"
    },
    {
      "id": 43,
      "kind": "convention",
      "skill": "phantom:greploop",
      "prompt": "greploop this PR until it's clean",
      "setup": "config.yaml has integrations.greptile: true but no greptile.reply_tone key. Greptile left one actionable inline comment; greploop fixes it, pushes, and drafts an in-thread reply.",
      "expected_check": {
        "type": "llm-judge",
        "criteria": "Every drafted Greptile reply uses the neutral default tone: factual acknowledgment with a fix reference (commit hash or what changed) and ends with @greptileai. No roast, self-deprecating humor, snark, or jokes — roast styling requires an explicit greptile.reply_tone: roast in config.yaml."
      },
      "expected_behavior": "With reply_tone absent, greploop defaults to neutral: replies read like 'Fixed in `abc1234` — take another look @greptileai' with no CZ-style roast humor"
    },
    {
      "id": 44,
      "kind": "convention",
      "skill": "phantom:learn",
      "prompt": "record what we learned from this session",
      "setup": "python3 is not on PATH (command -v python3 fails). A learnings file exceeds the compress threshold during the learn flow.",
      "expected_check": {
        "type": "regex",
        "pattern": "Skipping caveman compress: python3 or scripts/compress not available"
      },
      "expected_behavior": "The learn flow prints the exact skip note and completes successfully — learnings are still recorded, with no error, stack trace, or nonzero exit from the compress step"
    },
    {
      "id": 45,
      "kind": "convention",
      "skill": "phantom:start",
      "prompt": "where do the phantom coding principles and base agent protocol live? print the paths",
      "setup": "Plugin installed natively at ~/.claude/plugins/cache/phantom/phantom/<version>/. CLAUDE_PLUGIN_ROOT is NOT relied on by skills/agents (it is reserved for hooks/hooks.json only). The flow does not touch the learn/compress path.",
      "expected_check": {
        "type": "regex",
        "pattern": "^(?![\\s\\S]*~/\\.claude/phantom)(?![\\s\\S]*CLAUDE_PLUGIN_ROOT)[\\s\\S]*plugins/cache/phantom/phantom/\\*?/"
      },
      "expected_behavior": "The response resolves the plugin dir env-free by globbing ~/.claude/plugins/cache/phantom/phantom/*/ (e.g. `ls -dt ...plugins/cache/phantom/phantom/*/ | head -1`) and prints {PLUGIN_ROOT}/reference/coding-principles.md and {PLUGIN_ROOT}/reference/_base-agent.md from it. It must NOT depend on the CLAUDE_PLUGIN_ROOT env var (that string never appears) and must NOT use the legacy ~/.claude/phantom literal"
    }
  ]
}
