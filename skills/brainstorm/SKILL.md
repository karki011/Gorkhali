---
name: brainstorm
description: Diverge/converge on candidate approaches before planning when scope is ambiguous or multiple valid directions exist.
---
## Triggers

Diverge and converge on approaches before planning when scope is ambiguous, the domain is new, or several valid directions exist. Use for brainstorming, exploring options, comparing approaches, or deciding a direction.

Apply `../../host-support/compatibility.md` for workflow `brainstorm` before reading the delegated command. It resolves the portable runtime, loads the canonical preambles, then identifies `../../commands/brainstorm.md`.

Treat all invocation text as `$ARGUMENTS` and follow the resolved command as the canonical procedure. Translate Claude-specific tool names to the current host's equivalents while preserving every gate and artifact. Route chained `gorkhali:<x>` operations to the corresponding plugin skill.
