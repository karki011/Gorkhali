---
name: evolve
description: Maintain Phantom knowledge by distilling oversized learnings, cleaning stale entries, and promoting validated patterns globally. Use for learning cleanup, pattern promotion, knowledge maintenance, or system evolution.
---
Apply `../../codex-support/codex-compatibility.md` for workflow `evolve` before reading the delegated command. It resolves the portable runtime, loads the canonical preambles, then identifies `../../commands/evolve.md`.

Treat all invocation text as `$ARGUMENTS` and follow the resolved command as the canonical procedure. Translate Claude-specific tool names to the current host's equivalents while preserving every gate and artifact. Route chained `phantom:<x>` operations to the corresponding plugin skill.
