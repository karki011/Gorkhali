---
name: health
description: Diagnose Phantom knowledge-system integrity, including stale learnings, corrupted sessions, missing artifacts, broken edges, and index drift. Use when Phantom itself seems broken or when asked to check or diagnose Phantom health.
---
Apply `../../codex-support/codex-compatibility.md` for workflow `health` before reading the delegated command. It resolves the portable runtime, loads the canonical preambles, then identifies `../../commands/health.md`.

Treat all invocation text as `$ARGUMENTS` and follow the resolved command as the canonical procedure. Translate Claude-specific tool names to the current host's equivalents while preserving every gate and artifact. Route chained `phantom:<x>` operations to the corresponding plugin skill.
