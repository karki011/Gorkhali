---
name: hound
description: Investigate unknown causes of bugs, regressions, wrong behavior, and mysterious failures, producing a forensic report without guessing at fixes. Use when something is off, started after a change, or has no clear error; use fix for known failures.
---
Apply `../../codex-support/codex-compatibility.md` for workflow `hound` before reading the delegated command. It resolves the portable runtime, loads the canonical preambles, then identifies `../../commands/hound.md`.

Treat all invocation text as `$ARGUMENTS` and follow the resolved command as the canonical procedure. Translate Claude-specific tool names to the current host's equivalents while preserving every gate and artifact. Route chained `phantom:<x>` operations to the corresponding plugin skill.
