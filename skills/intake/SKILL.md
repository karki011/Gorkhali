---
name: intake
description: Capture originator intent as dual-readable intent.md for a later start; NOT for planning (start/brainstorm), implementing (execute), or shipping (wrap).
---
## Triggers

Capture a product, ops, or incident idea as intent.md before Gorkhali plans or implements. Use when asked to write intent, file a proto-spec, or record an incident as intent.

Apply `../../host-support/compatibility.md` for workflow `intake` before reading the delegated command. It resolves the portable runtime, loads the canonical preambles, then identifies `../../commands/intake.md`.

Treat all invocation text as `$ARGUMENTS` and follow the resolved command as the canonical procedure. Translate Claude-specific tool names to the current host's equivalents while preserving every gate and artifact. Route chained `gorkhali:<x>` operations to the corresponding plugin skill.
